import { SnitchCountPipe } from './snitch-count.pipe';

describe('SnitchCountPipe', () => {
  it('create an instance', () => {
    const pipe = new SnitchCountPipe();
    expect(pipe).toBeTruthy();
  });
});
